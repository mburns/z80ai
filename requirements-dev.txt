# Everything needed to build the binaries and run the test suite.
# Training additionally needs PyTorch, which nothing here requires:
# the emulator, the reference model and every build script are NumPy-only.
numpy>=1.24
pytest>=7.0
ruff>=0.6
